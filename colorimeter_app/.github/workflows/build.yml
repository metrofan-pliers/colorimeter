name: Build APK

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.6'
          channel: 'stable'

      - name: Install dependencies
        run: |
          cd colorimeter_app
          flutter pub get

      - name: Build debug APK
        run: |
          cd colorimeter_app
          flutter build apk --debug

      - name: Build release APK
        run: |
          cd colorimeter_app
          flutter build apk --release

      - name: Upload APK files
        uses: actions/upload-artifact@v4
        with:
          name: apk-files
          path: |
            colorimeter_app/build/app/outputs/flutter-apk/app-debug.apk
            colorimeter_app/build/app/outputs/flutter-apk/app-release.apk
          retention-days: 30

      - name: Create Release
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v1
        with:
          files: |
            colorimeter_app/build/app/outputs/flutter-apk/app-release.apk
            colorimeter_app/build/app/outputs/flutter-apk/app-debug.apk
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
